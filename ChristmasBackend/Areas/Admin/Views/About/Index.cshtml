﻿
@model AboutVM
@{
	ViewData["Title"] = "Index";
}

<div style="margin-right:100px" class="container mt-5">
	<h1 class="text-center mt-3 mb-3">About</h1>
	<div class="table-responsive">
		<table class="table table-striped">
			<thead>
				<tr>
					<th>
						Image
					</th>

					<th>
						Title
					</th>
					<th>
						Description
					</th>
					<th>
						Video
					</th>
					<th>
						Operations
					</th>

				</tr>
			</thead>
			<tbody>

				<tr>
					<td class="py-1">
						<img style="width:200px; height:150px" src="~/img/@Model.Image" alt="image" />
					</td>

					<td style="white-space:normal">
						@Html.Raw(Model.Title)
					</td>
					<td style="white-space:normal">
						@Model.Description.Substring(0,35) ...
					</td>
					<td style="white-space:normal">
						@Model.Video.Substring(0,15) ...
					</td>

					<td>
						<a class="btn btn-info" asp-action="Detail" asp-route-id="@Model.Id">Detail</a>
						<a class="btn btn-primary" asp-action="Edit" asp-route-id="@Model.Id">Edit</a>

				</tr>


			</tbody>
		</table>
	</div>
</div>

